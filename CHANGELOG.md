# 更新日志

> 本文件是 PersonalMediaManager（PMM）的版本更新记录。版本号体系（产品 / 后端 / 数据库 / 前端四套）详见 [README §八](README.md#八版本号体系)；数据库 schema 版本与 migration 的映射见 [db/version-map.json](db/version-map.json)。

## [未发布]

### 新增
- 扫描待确认网页提醒与任务取消（后端 0.11.0 / 前端 0.8.0）：手动全扫、周期/实时自动扫描与单目录扫描产生待确认文件时，网页右下角显示文件名提醒，点击可直接前往待确认队列；可在「设置 → 常规」关闭，默认开启；排队或归档前处理中的文件可在处理详情主动取消，正在执行的管线会安全停止并记为「已取消」（进入归档文件操作后为保证磁盘一致性不再允许取消）
- 托盘右键菜单新增「扫描硬盘」：点击后直接扫描全部已启用监控目录，复用现有全量扫描、去重和全局扫描互斥逻辑；扫描期间禁用重复点击，服务停止时自动取消，完成后以托盘气泡反馈目录数与入队文件数
- 强制匹配增「文件名 / 文件夹名 {tmdb-NNN} 标记」识别（后端 0.4.3 / 前端 0.3.2）：在原 `pmm.txt` / TMDB URL 之外，新增一种最省事的锚定方式——把 Plex 官方匹配标记 `{tmdb-NNN}` 直接写进**文件夹名或文件名**（如目录 `刀剑神域 (2012) {tmdb-45782}` 或文件 `流浪地球 (2019) {tmdb-535292}.mkv`，兼容 `[tmdb-]` / `[tmdbid-]` / 大写，文件名扩展名不影响），即**只锚定该 TMDB 条目的 id**，电视剧 / 电影类型、季号、集号**仍由规则引擎正常识别**（与贴 URL 时连类型/季一并锁定不同）。类型解析先用规则识别结果拉 TMDB，若 tv/movie 猜反（两者是独立命名空间，错则 404）自动翻另一类型兜底，两者皆失败才转人工。因 `{tmdb-NNN}` 正是本工具归档落点产出的标准标记，**已归档作品整目录 / 单个电影文件回投监控目录会被自动认出、零 TMDB 搜索零 AI**。自文件目录逐层上溯至监控根，优先级 `pmm.txt` > `.url` > 文件名标记 > 文件夹名标记（前两者信息更全），跨层越靠近文件越优先（文件名标记仅在文件所在目录这一层识别）；设置→强制匹配说明页同步补充该用法
- 字幕下载（后端 0.2.0 / 数据库 0.2.0 / 前端 0.2.0）：对已归档完成的媒体记录手动触发外部字幕源搜索与下载，全程手动选择、零自动触发。首发 Assrt（伪·射手网）字幕源（架构按多源策略设计，预留扩展）；入口在历史记录 / 媒体详情 / 媒体库详情的行操作「下载字幕」，弹窗内预填原始文件名搜索词（可编辑重搜）→ 候选列表 → 展开文件列表 → 选择下载；落地命名复用归档管线规范（`{视频基名}.{语言码}.{扩展名}`，同名序号避让不覆盖），语言码按文件名识别、失败回退源站语言描述映射；新增 设置→字幕 页（Assrt Token 加密落库、连通性测试显示剩余配额、可选走代理）；成功下载落 Subtitle_Download 记录并在弹窗内提示已下载避免重复；服务端按候选 Id 重取直链（前端不传 URL，杜绝 SSRF）、扩展名白名单校验、限流 1 次/3.2 秒守住 Assrt 配额

### 修复与优化
- 任务调度审计去噪（后端 0.1.2）：WebhookRetryJob 每分钟兜底扫描空转（无待重试投递）时不再写 Audit_ScheduledTaskRun——此前每分钟两次落库使该表每年膨胀约 50 万行、仪表盘任务时间轴被「处理 0 个」记录刷屏；失败或有处理量时照常落库，故障可见性不受影响
- 调度运行记录自动留存清理：LogRetentionJob 每日顺带删除超过 30 天的 Audit_ScheduledTaskRun 行（含进程崩溃残留的 Running 僵尸行），补齐该表此前缺失的自动清理路径

## [0.1.0] - 2026-06-12

> **基线版本**：项目此前开发期的全部功能演进整合为本条记录，四套版本号自此统一从 0.1.0 重新计数（配合仓库迁移从最初版重新开始）。

### 移除
- 彻底移除 macOS 支持（2026-06-11 决策，后端 0.1.1）：产品仅支持 Windows。Launcher 收敛单 TFM net10.0-windows，删除 Mac 托盘/自启/单实例/全盘访问实现与打包脚本，CI 删除 macOS agent，文档与冒烟清单收敛仅 Windows；`IPlatformTray` 等平台抽象接口保留（隔离 OS 专属 API、便于单测）。

### 文件监控与扫描
- 实时监听监控目录，文件写入完成探测（大小稳定）后入队，单消费串行处理保障 SQLite 安全
- 监控目录增删改即时重建监听、运行时生效，支持连通性测试与在途处理守护
- 忽略规则可配，内置种子拦截 .torrent/.aria2/.!ut 等下载器中间/辅助文件误入管线
- 媒体扩展名动态字典化，WebUI 增删即生效，取代硬编码后缀表
- 周期全量扫描 + 单目录手动扫描 + 强制重扫（自动重投失败历史）；不可读子目录跳过、不中断整次扫描
- 网络盘可达性周期探测，断连暂停、恢复自动重建监听并补扫；进程重启后搁浅任务自动恢复且双向核验
- 处理队列页全程可视：新发现文件入队即落行，各阶段状态实时可见

### 媒体名称解析（规则引擎 + AI 兜底）
- 规则引擎分层解析：用户自定义正则按优先级优先、内置规则兜底，按置信度评分决定直通/送审
- 内置 20+ 条可编辑种子规则，覆盖国产剧、综艺日期、全N集、Episode N、第N章回、方括号双段、Part/Cour/Vol、YYMMDD 短日期等盲区命名
- 路径段全链解析：监控根至文件名逐段提取线索，覆盖 PT 站命名、双集合并、总季/总集后缀剥离、中文噪声词清洗
- 季集识别增强：中文数字/罗马数字/篇章季标题对照 TMDB 季名、Season NN 目录取季、小数集号（11.5 话）全层守护不截断、第 0 集/特别篇归 S00/E00；缺季号降分送审而非直接失败，TMDB 单季剧自动补季号
- AI 兜底解析支持 Ollama/Qwen/DeepSeek/OpenAI 兼容四类供应商，协议与解析器分离，结构化 JSON 输出，请求节流
- AI 多级升级链：结果不满意/接口异常/限流自动逐级升级更高级供应商，置信度阈值可配，滚动失败窗口自动熔断禁用，季集越界钳制
- 同目录剧集复用解析结果、跳过重复 AI 调用（仅子目录+TV+标题相似防串台）；中文名 TMDB 搜不中时按 AI 输出别名逐个重搜（国漫/日漫兜底）
- 解析测试集：用例管理+暂存区灌入、回归引擎+基线批准+Diff 对比、AI Triage 判定与 SuggestRule 一键转正式规则

### TMDB 元数据
- TMDB 客户端封装搜索/详情/连接测试，v4 Read Access Token 加密落库，候选按四维择优排序
- 搜索回退链：带年份→去年份→回退语言逐级重试；搜索语言与速率为可配活旋钮，429 限流退避带上限钳制，改语言自动清缓存
- 搜索与详情结果本地缓存，海报/图片归档前缓存到本地，前端走本地地址不回源
- 文件夹↔剧集映射持久化：同文件夹后续集复用本地数据直接跳过 TMDB 搜索，重启后与规则路径均生效
- 处理时间线标注数据来源（内存/本地缓存/远端），缓存命中不污染简介字段
- 全局 HTTP 代理设置，TMDB/升级检查/按供应商勾选的 AI 调用可走代理出网

### 分类与归档
- 分类定义+匹配规则双层模型：JSON 条件树求值（类型/出品地区/原始语言/年份等），地区与语言可搜索下拉，被引用分类禁删
- 归档落点统一解析：目标文件夹按 TMDB 规范名+{tmdb-NNN} 标记生成并复用，根治同一作品散落多文件夹；多季季文件夹带季标题与季年份
- Plex/Emby/Jellyfin 兼容命名：电影/剧集/特别篇/多版本完整规范，目录与文件名嵌 {tmdb-id} 标记强制刮削匹配
- 文件搬移原子化：同卷重命名、跨卷复制+删除+.tmp 中转，路径安全守护防越界；字幕按语言映射改名随行，nfo 元数据与海报落地
- 归档标题语言回退链：中文名→TMDB 原文名→解析名；nfo/Webhook 等收尾失败降级容错，不整条判失败、标记「待补元数据」
- 归档前磁盘空间检查，同名多版本策略可配，升级替换自动失效旧记录，失败批量重试
- 整理演练（DryRun）不落盘预演完整归档路径；手动整理入口与单文件「测试路径/手动移动复制」诊断

### 人工确认与历史
- 待确认队列按作品组流水线展示，含去向预览、入队原因标签、忽略规则与临时格式拦截、文件存在检查、列排序与常驻全选
- 整页确认抽屉：季/集编辑、按关键词/类型/年份手动搜 TMDB、绑定或手填 TMDB ID 预览、多候选全集落库展示
- 同目录兄弟剧集一并批量确认（同一剧集+分类，自动分片）；绝对集号⇄分季双向重映射（多季合并连续编号/按 TMDB 拆回各季）
- 人工填错可退回重改；确认/绑定带 RowVersion 乐观并发，批量操作带归档进度遮罩
- 历史页聚焦日志与失败兜底：过滤分页、失败重投保留监控根路径段、重新处理/删除支持单条/批量/整剧、待补元数据徽标
- 一键撤销最近归档（带身份校验）；处理中断自动收尾，不卡归档中状态
- 媒体库海报墙+作品详情页：TMDB 富化信息、分季分集、关联作品、库内搜索、该作品全部文件记录与批量操作

### Webhook 通知
- 订阅管理+事件多选+测试发送；全局总开关默认关闭，关闭时不产生投递记录
- 出站投递走 Outbox 队列：HMAC 签名、指数退避重试状态机、周期重投任务、重试档位可配+手动重试端点
- 事件覆盖归档成功/失败/跳过、待确认产生、备份失败、磁盘低空间、网络盘断连、AI 全不可用等
- 投递记录分页查询，订阅卡片展示真实成功/失败统计

### 审计与监控
- 操作审计与外部调用统计落库，定时任务 24 小时运行历史入表可查
- AI 调用全量审计：模型/token/置信度/HTTP 状态/升级链标识/请求响应原文，分页调用日志+留存双闸（天数+单供应商行数）每日清理
- AI 监控页聚合近 7 天调用量与成本置信度，升级链路阶梯可视化
- 健康检查多项并行：数据库/归档磁盘/网络盘/AI 可用性/备份状态等，磁盘警戒阈值可配
- 主动告警磁盘低空间/网络盘断连/AI 全不可用，带抑制窗口防告警风暴，阈值范围校验防配置失效
- Serilog 三路输出（控制台/文件/SignalR 实时日志页），30 天日志保留任务，当前过滤可导出；敏感信息写入与回放读取双侧脱敏

### 备份与数据安全
- 自动备份任务：每日 VACUUM INTO 在线快照，数据库与 DataProtection 密钥环同组打包，按保留份数滚动清理
- 备份失败独立告警+仪表盘备份健康检查；手动导出/导入（防 Zip Slip，暂存+启动期原子换库规避 WAL 热替换失效）
- SQLite 启用 WAL+busy_timeout 根治 database is locked；启动期数据库自愈（预防性 checkpoint+损坏误报单次重试）
- 敏感字段（TMDB Token/AI ApiKey/GitHub PAT）一律 DataProtection 加密落库，不进配置文件与日志
- 内容去重接通：采样哈希识别重复文件

### 软件更新
- 后台周期检查 GitHub Releases 最新版本，与产品版本号按 SemVer 段级比对，周期默认 24 小时可配
- 发现新版托盘气泡提示，同版本去重不重复弹
- 可选 GitHub PAT 加密落库（推荐 Fine-grained 只读），应对私仓与限流
- 设置页可改启用开关与检查周期

### 认证与安全
- 首启初始化向导创建管理员，JWT 登录+滑动续期中间件，BCrypt 密码哈希
- 全局默认拒绝鉴权策略，未初始化时仅拦 API、放行 SPA 外壳；诊断端点收紧到管理员
- 边界声明式校验全量铺开：请求 DTO DataAnnotations（含集合元素递归），校验失败统一信封返回
- API 响应统一信封：极简 3 码（0/1000/9000）+requestId 全链贯穿
- 写操作乐观并发 RowVersion（分类/规则/监控目录/确认绑定），并发冲突明确报错
- 安全自检自动化：ReDoS 防护、异常文案脱敏、鉴权与源码守卫测试

### Launcher 托盘
- 单文件 self-contained exe：前端产物与 appsettings 嵌入程序集，发布产物收敛为单一 exe
- Windows 原生 NotifyIcon 托盘：启停服务、双击/菜单打开 WebUI、复制局域网访问地址、端口与数据库位置配置对话框，图标随系统明暗主题切换，Tooltip 实时状态
- 单实例守护（Mutex+命名管道二启唤起）、开机自启注册、防火墙规则首启注入（UAC 拒绝降级为气泡提示）
- 锁定 asInvoker 不提权，保障网络盘归档可达；启动致命错误写 tray-crash.log+弹窗兜底
- 数据目录绿色化：exe 旁 data 目录随程序走，不可写自动回退 %LocalAppData%；local.json 仅覆盖端口与数据库路径，JSON 损坏自愈
- 多 TFM 预留 macOS 平台抽象与骨架（本期暂缓），禁入跨平台 UI 框架

### WebUI 前端
- Vue 3+Vite+Element Plus+Pinia 高保真 SPA，侧栏按采集/识别归类/集成/系统四组语义分组
- 页面全集：仪表盘、处理队列、待确认、历史、媒体库与作品详情、实时日志、解析规则、解析测试集、监控目录、分类、TMDB/AI/Webhook/代理/更新各设置页
- openapi-fetch 类型化 API 客户端，schema.d.ts 由 OpenAPI 生成并设 CI 漂移门禁
- SignalR 实时推送日志流与任务状态，处理时间线逐步骤展示
- 仪表盘单一视图整合统计/最近处理/健康检查/任务历史
- 静态资源缓存分级发新版免强刷，深浅主题适配，表单 autocomplete 防密码管理器误填

### 工程与质量
- 7 src+6 tests 严格单向引用矩阵，Domain 层零外部依赖，引用红线脚本扫描守护
- 前端 .esproj 并入解决方案深度集成：dotnet build/publish 自动触发 vite 构建并嵌入 wwwroot，带增量短路，根治 MSB3030
- 双管道 CI/CD：Azure Pipelines 承担 PR 构建/测试/红线扫描（self-hosted 双 agent），GitHub Actions 仅发版打包（tag 进 Release、main 仅产 7 天 Artifact）
- 四套版本号体系（产品/后端程序集/数据库 schema/前端）随 git 元数据自动注入，关于页统一展示
- db/version-map.json 维护 schema 版本↔MigrationId 映射，运行时 floor 反查已应用版本并识别待迁移状态
- Scalar OpenAPI 文档+Controller 注释规范 CI 守门；媒体状态机 13 状态穷举回归；多轮全栈审计（契约对齐/死按钮/假数据/并发/安全）全部闭环
- 项目更名 Plex→Personal 全栈收尾：运行时标识（数据根/JWT/自启/Mutex/防火墙等）全量切新名，产品缩写 PMM 不变

### 基线统计
- 整合此前全部 437 次提交的演进为本条基线记录
- 数据库历经 29 个 EF migration、16 个 schema 版本窗口演进，随基线整合合并重建为单一 Init 迁移（schema + 种子全量保留）
- 7 个 src 项目+6 个 tests 项目+1 个前端 .esproj 工程，严格单向引用
- 817+ 自动化测试，全部基于本地内存数据库与文件 IO，不依赖外部服务
